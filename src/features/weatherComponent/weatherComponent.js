import React from "react";
import weatherStyle from "./weatherComponent.module.scss";

const Weather = () => {
    return(
        <div id={weatherStyle.weather}>
            WEATHER
        </div>
    )
}

export default Weather;